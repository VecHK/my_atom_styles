我的 Atom `styles.less`，基于 `Nord Syntax` 和 `One Dark`

字体使用 **Inziu Iosevka**

---

my Atom `styles.less`, base on `Nord Syntax` and `One Dark`

code font using **Inziu Iosevka**

---

Preview:

![image](./preview.png)
